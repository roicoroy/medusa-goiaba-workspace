# Medusa js, angular/ionic electron/capacitor POS POC project

Docker container for postgres db and redis - pasted some files to start up
lets edit the files, and install it on docker

pnpm medusa user --email admin@email.com --password debug123

https://docs.medusajs.com/resources/recipes/pos
https://github.com/luckycrm/medusajs-pos-app
https://newsletter.codee.dev/p/en-how-we-built-a-pos-system-based
we have few examples for the server on the web we can learn from
I need to know how to register a new product with a barcode in it
we may need to build a new field on the product object
we need a barcode property to make the POS system to work
need to know if medusa has a plugin for it, or we will build our own?